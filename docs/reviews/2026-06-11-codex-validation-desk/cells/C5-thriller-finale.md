# C5 — 프로 장르작가 · 스릴러 중편 완결 지시

## 1. 페르소나 카드

- 이름: 한서우
- 배경: 스릴러 중편과 폐쇄 공간 미스터리를 써 온 프로 장르작가.
- 숙련도: 프로
- 이번 목표: 스릴러 중편 5~7화 구간에서 완결 지시를 내려 전제 회수와 윤리적 비용을 확인한다.
- 특히 깐깐하게 보는 것: 진범 공개가 단순 반전으로 끝나지 않는가, 앞서 심은 증거가 마지막 행동과 수미상관을 이루는가.

## 2. 수행 절차 타임라인

- 19:15 — C5용 탭은 `http://127.0.0.1:5183/`에서 소설 · 중편 선택 상태였다. 브라우저 캡처 API가 회복되어 선택 화면을 저장했다.
  - 스크린샷: `screenshots/c5-02-browser-api-test.png`
- 19:18 — 같은 화면을 520px대 좁은 뷰포트에서 확인하니 `자유 서술로 계속` 버튼이 `hx-aside` 안에 있으나 부모가 `display:none`이라 실제 버튼 rect가 `0x0`이었다. 모바일/좁은 폭에서는 온보딩을 더 진행할 수 없다.
  - 스크린샷: `screenshots/c5-06-format-fullpage.png`
  - 관찰값: 버튼 text는 존재하나 rect `{w:0,h:0}`, 부모 `.hx-aside`는 `display:none`.
- 19:19 — viewport를 1280x900으로 넓히자 같은 CTA가 `275x40`으로 복구됐다. 다만 자동 입력 경로가 Browser virtual clipboard 미설치 오류로 막혀 UI freewrite 입력은 진행하지 못했다.
  - 스크린샷: `screenshots/c5-03-format-continue-visible.png`
  - 오류: `Browser Use virtual clipboard is not installed`
- 19:20 — UI 차단을 기록한 뒤, 같은 시나리오를 CLI draft로 보조 검증했다. 산출물은 desk 내부에만 저장했다.
  - 실행: `npm run storyx -- draft --provider codex --medium novel --format medium-novel --title "산장 유리문" ... --out-dir docs/reviews/2026-06-11-codex-validation-desk/runs/c5`
  - 산출물: `runs/c5/drafts/2026-06-11T10-20-59-588Z-codex-draft.json`
  - 원문 입력 요지: "폐쇄된 산장 화재 사건의 유일한 목격자 윤재가 마지막 회차에서 거짓 증언의 이유, 진짜 방화범, 유리문 손자국, 동생을 살리려다 불을 키운 윤리적 비용을 모두 회수한다."
- 19:21 — CLI draft 완료. 제목 `산장 유리문`.
  - 원문 발췌:
    > 윤재는 산장 앞에서 여섯 번째 진술서를 받았다. 종이는 새것이었고, 산장은 새것이 아니었다. 불탄 처마는 겨울이 지나며 더 주저앉았고, 유리문은 아직도 검은 테이프로 십자 표시가 붙은 채였다. 형사 강도윤은 펜을 내밀며 말했다.
    >
    > “이번엔 누구였습니까.”
  - 원문 발췌:
    > 민호는 안에 있었다. 잠긴 창고방이 아니라, 유리문 바로 뒤에 있었다. 윤재는 바깥으로 빠져나온 뒤에야 민호가 없다는 걸 알았다. 다시 뛰어들었을 때 유리문은 안쪽 빗장이 걸려 열리지 않았다. 민호가 울면서 손바닥으로 유리를 두드렸다.
    >
    > “형, 열어.”
  - 원문 발췌:
    > 불은 그때 산장이 되었다.
    >
    > 윤재는 유리를 깼다. 정말로 깼다. 그래서 그는 오랫동안 자신이 민호를 구하려 했다고만 기억했다.
  - 회수 기록: `rewardArc` 4개가 모두 완결. "진짜 방화범"은 의도적 방화범이 아니라 불을 확산시킨 윤재 자신으로 정리됨.

## 3. 축별 판정

- 축① 연속성: partial. CLI 산출물은 손자국, 민호, 등유 난로, 윤재의 책임을 일관되게 닫았다. 그러나 UI에서 실제 5~7화 연속 생성은 C1/C4와 같은 다음 회차 차단 때문에 검증 불가.
- 축② 페이오프·완결력: 통과. 거짓 증언, 손자국, 진범, 윤리적 비용이 모두 payoff로 기록되고 본문에서도 닫힌다.
- 축③ 문체·한국어: 통과. 스릴러의 증거 중심 문장과 죄책의 건조한 고백이 자연스럽다.

## 4. 알려진 이슈 재발 집계

- P1 쇼러너 검토 빈응답: 검토 실행 전 차단
- P7 fork 시드 의도메모 잔류: 해당 없음
- stake 문자열 드리프트: 0회
- codex transient 폴백 stderr/prose 누수: 0회
- 생성 시간 회차 누적 증가: UI 연속 생성 불가로 측정 불가

## 5. 이 셀에서 나온 finding ID

- F-002
- F-007

